# Experiment-2
# Aim - Study of Lists in Python
# Theory
In Python, a list is a built-in data type for storing ordered collections of items. Lists are mutable and can include elements of any type.List literals are written within square brackets [ ].List index begins at zero and ends at the length of the list minus one.Various built-in functions like len(), max(), min(), insert(), .append(), remove(), .sorted, etc help in manipulation of strings .

**Algorithm:**

 Finding Maximum, Minimum, and Sorting Marks List (DIY)

1. Accept marks from the user in the form of a list.
2. Use built-in functions to find the maximum and minimum marks.
3. Sort the list in ascending order.
4. Display the maximum, minimum, and sorted list.

---

Finding Average

1.find the sum using sum() function and then divide it by the length of the list len() function

---

**Grocery List Management Program (DIY)**

**Algorithm:**

1. Create an initial grocery list with predefined items.
2. Allow the user to add new items to the list.
3. Allow the user to remove unwanted items from the list.
4. Display the updated grocery list.
   
Conclusion - Learnt basics of lists in Python
